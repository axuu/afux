# 复制为 secrets.yaml 后填写真实值；不要提交 secrets.yaml。
wifi_ssid: "REPLACE_WITH_WIFI_SSID"
wifi_password: "REPLACE_WITH_WIFI_PASSWORD"

# 生成命令：openssl rand -base64 32
api_encryption_key: "REPLACE_WITH_GENERATED_KEY"

# 体脂秤的 BLE MAC 地址；它不是认证密钥，但属于设备实例配置。
scale_mac_address: "AA:BB:CC:DD:EE:FF"
